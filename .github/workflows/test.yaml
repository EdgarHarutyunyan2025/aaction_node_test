name: CI

on: [push]

jobs:
  run-docker-action:
    runs-on: ubuntu-latest 
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Run Docker-based Action
        uses: /home/edgar/DevOps/actions/test_node 
        with:
          message: 'Hello, Docker!' 
